# Changelog

## v1.11.1 (2026-09-17)

### 📦 Version

- 项目版本从 `1.11.0` 升级到 `1.11.1`，同步 package、Tauri、Rust crate 与 Cargo lockfile。

### 🐛 Fixed

- 更新日志支持 Markdown 渲染及验证记录折叠，过滤不受支持的 HTML 与链接；浮动提示显示解析后的摘要，不再展示源码。
- 关于模态框加宽并限制可用高度，更新日志独立滚动，下载按钮不再挤占正文宽度，关闭与更新操作保持可达。

### 🧰 Chore

- 开发环境的关于模态框提供长日志、Markdown、短日志和空日志预览；使用正式展示组件，禁用真实检查和安装，生产构建排除预览模块。

### ✅ Verify

- 类型检查与生产构建通过；Markdown 渲染、默认折叠、HTML/链接过滤、摘要和空日志的静态渲染断言通过，生产产物不包含开发预览模块与模拟数据。
- Markdown 渲染器按需加载；Vite 保留主包超过 500 kB 的提示。未启动本地应用，模态框在不同窗口尺寸下的实际布局与桌面交互待手动验证。

## v1.11.0 (2026-09-17)

### 📦 Version

- 项目版本从 `1.10.1` 升级到 `1.11.0`，同步 package、Tauri、Rust crate 与 Cargo lockfile。

### 🎉 Added

- 桌面更新支持 GitHub 连接失败后自动使用 gh-proxy 加速源检查和下载，检查每个源最多等待 10 秒，下载单次最多等待 5 分钟。
- 更新详情显示来源、回退状态、真实下载进度和已下载大小；代理下载复用原版本与签名，签名校验失败时停止安装。

### 🐛 Fixed

- 修复标题栏检查更新图标静止的问题，检查、下载和安装阶段显示旋转动画，遵循系统减少动态效果设置；检查期间可打开详情。
- 避免重复更新操作及迟到事件覆盖状态，保留原生错误原因；未知下载大小不再显示虚假百分比。

### ✅ Verify

- 类型检查、生产构建、Windows 桌面检查通过；Vite 保留主包超过 500 kB 的提示。
- 3 项 Rust 测试覆盖下载地址限制与网络回退分类；前端内存断言覆盖防重入、迟到事件、回退进度重置、未知总大小及安装阶段。
- 已确认 gh-proxy 可读取发布清单并通过 GitHub API 资产地址访问 Windows MSI（HEAD 200）；尚未执行真实下载验签安装、断网端到端测试及 macOS/Linux 运行验证。

## v1.10.1 (2026-09-16)

### 📦 Version

- 项目版本从 `1.10.0` 升级到 `1.10.1`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.10.1`。

### 🐛 Fixed

- 修复 GitHub Release 仅显示固定构建提示、缺少实际更新说明的问题；发布时按版本标签精确提取 CHANGELOG，并保留安装提示与完整记录链接。
- 发布前检查版本一致性和有效更新条目，阻止缺失、重复或空说明的发布；多平台共用同一份正文，重跑时同步已有 Release 说明。
- 保留原版本的验证记录与已知限制，折叠展示，避免过滤内部验证分类时遗漏实际使用限制。

### ✅ Verify

- 类型检查、更新说明提取与异常输入断言、版本一致性检查通过。
- v1.10.0 线上 Release 正文已补齐并回读核对，17 个上传资产保持不变；新的多平台发布工作流尚未在 GitHub Actions 运行。
- 已按各自发布 tag 补齐并回读核对 v1.3.0 至 v1.10.0 的全部 11 个 Release 正文；所有上传资产、标签和发布元数据保持不变，验证记录中的使用限制已保留。

## v1.10.0 (2026-09-16)

### 📦 Version

- 项目版本从 `1.9.1` 升级到 `1.10.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.10.0`。

### 🎉 Added

- 生图连接支持获取当前 Endpoint 与 API Key 下的模型列表，筛选支持的 13 个完整模型 ID，涵盖 GPT Image 2、2.5、Flare、Sunburst 及对应的 1k、2k、4k 版本。
- 获取失败或渠道未返回目标模型时，可手动输入支持的完整 ID 并确认使用；明确标记未经模型列表验证，确认过程不会发起生图请求。

### 🔄 Changed

- 新配置默认使用 `gpt-image-2.5-flare`，已有有效模型选择继续保留。
- 模型 ID（包括分辨率后缀）在保存、生成、编辑与桌面端请求中原样传递；质量和尺寸设置不自动改写模型 ID。
- 切换地址、密钥或关闭设置时清除临时发现与手动确认状态，忽略旧请求的迟到响应。

### 🐛 Fixed

- 修复保存设置、调整快捷设置和生成快照时将所选模型覆盖为固定模型的问题，并同步扩展桌面端模型校验。

### ✅ Verify

- 类型检查、生产构建与 Windows 桌面检查通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- 全部 13 个模型的规范化、持久化、前后端白名单一致性和 26 个生成/编辑请求的模型 ID 断言通过。
- 尚未验证真实渠道生图；带分辨率后缀的模型与 `size` 参数的优先关系由渠道决定，本次未扩展 2.5 的质量档位。

## v1.9.1 (2026-09-02)

### 📦 Version

- 项目版本从 `1.9.0` 升级到 `1.9.1`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.9.1`。

### 🐛 Fixed

- 修复单图打开大图后缺少复制、下载和图片变换工具栏的问题；单图与多图现在共用每轮独立的预览工具栏，单图不会显示无意义的翻页按钮。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过。
- `git diff --check` 通过。

## v1.9.0 (2026-09-02)

### 📦 Version

- 项目版本从 `1.8.1` 升级到 `1.9.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.9.0`。

### 🎉 Added

- AI 连接新增“自动、低、中、高”推理程度选择，设置会持久化保存，并同时作用于 AI 规划和提示词优化。
- 新增 AI 规划进度卡片，显示当前模型、规划目标、已用时间以及准备、等待响应、接收结果、结构校验等真实阶段，并支持中途停止。
- 图片重试改为在原结果卡片内执行；生成完成后可切换或并排比较上一版与新版本，并且必须二选一后才能再次重试。

### 🔄 Changed

- AI 规划统一使用非流式完整 JSON 响应，只展示可验证的请求与处理状态，不再展示或模拟模型思维链。
- 不支持指定推理程度的兼容 Endpoint 会自动回退到模型默认设置、继续当前任务并明确提示，不再直接中断规划。
- 多图预览改用批次内原生相邻图片导航；翻页按钮固定在页面两侧并扩大为圆形控件，底部复制、下载和变换工具栏统一对齐与触控尺寸。
- 中英文 README 增加 SceneMeld Web 线上体验地址，并同步修正 Cloudflare Pages 部署说明。

### 🐛 Fixed

- 修复重新生成图片时新开一轮对话的问题；普通图片和分镜图片都会在原卡片内保留上一版，分镜重试会尽可能继续引用前一镜头。
- 修复多图预览首次打开时可能没有左右翻页按钮，以及按钮随图片缩放、比例和布局测量发生位移的问题。
- 修复图片预览底部工具栏部分变换图标未在按钮框内居中、窄屏触控区域不足的问题。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过。
- `git diff --check` 通过。

## v1.8.1 (2026-09-01)

### 📦 Version

- 项目版本从 `1.8.0` 升级到 `1.8.1`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.8.1`。

### 🔄 Changed

- 图片复制与下载优先使用已经加载成功的稳定图片数据，不再为缓存预览重复请求临时地址。
- Web 与桌面端内容安全策略统一允许读取应用内部的 `blob:` 与 `data:` 图片数据。

### 🐛 Fixed

- 修复图片已经正常显示时，复制仍可能误报“应用暂时无法取得原始图片数据”的问题。
- 修复图片预览层遮挡“复制成功”等反馈提示的问题，复制结果现在始终显示在预览上方。
- 修正原始图片确实不可用时的恢复操作与提示：不再提供必然失败的下载入口，并明确引导用户重试、重新打开结果或重新生成。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过。
- `git diff --check` 通过。

## v1.8.0 (2026-09-01)

### 📦 Version

- 项目版本从 `1.7.0` 升级到 `1.8.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.8.0`。

### 🎉 Added

- 新增统一的单图、多图与分镜生成模式选择；多图可控制生成数量，分镜可选择自动或预设镜头数，并在提交前显示本次规格摘要。
- 新增 `gpt-image-2` 原生规格映射：支持自动、横竖构图、方图与有效自定义比例，质量选项以中文显示，输出格式固定为 PNG。
- 新增图片复制恢复流程：桌面端优先使用原生图片剪贴板，失败时按权限、占用、环境不支持或图片不可用分别提供授权指引、重试与下载 PNG。
- 新增可持久化的 AI 规划适用场景，分别控制单图、多图与分镜；默认仅开启分镜，多图整批只调用一次规划模型并生成对应数量的差异化提示词。
- 新增分镜方案确认界面，可在正式生图前检查镜头、角色、场景和连续性提示。

### 🔄 Changed

- 重构底部编辑区为紧凑的下拉与数量控件，在窄窗口下保持核心参数完整可用，并减少工具栏对创作空间的占用。
- 提示词内的模式、比例、质量和数量指令优先于界面预设；非枚举但有效的比例会显示为“自定义 · 比例”，且不会改写默认设置。
- AI 辅助连接始终可配置，自动规划范围可全部关闭；手动提示词优化与规划范围解耦，只依赖完整的 AI 连接。
- 画面比例不匹配时不再静默自动重试；界面会显示请求尺寸与实际尺寸，并由用户显式选择是否按原设置重试。

### 🐛 Fixed

- 修复桌面端或浏览器复制图片只显示笼统失败提示、无法判断是否需要授权的问题。
- 修复部分比例仅改变界面状态但没有完整传入图片请求的问题；生成与编辑请求现在都会传递尺寸、质量和 PNG 输出格式，并校验实际返回比例。
- 修复多图规划逐张调用可能产生多条规划任务记录的问题；现在整批只规划一次，失败时提示并回退原提示词继续生成。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过。
- 直接调用本机 Cargo 时检测到现有 `target` 缓存的 Rust 1.88 / 1.91 工具链不兼容；项目标准桌面检查已通过仓库探测脚本规避该环境差异。
- `git diff --check` 通过。

## v1.7.0 (2026-08-24)

### 📦 Version

- 项目版本从 `1.6.0` 升级到 `1.7.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.7.0`。

### 🎉 Added

- 新增“内置规划 / AI 规划”分镜流程，支持结构化分镜计划、1 至 9 个连续镜头、统一画布规格、逐镜状态与单镜重做；AI 不可用时仍可使用应用内置模板。
- AI 规划支持共用图片连接或单独配置 Endpoint 与 API Key，并可显式获取上游模型列表、选择或手动填写对话模型；默认模型为 `gpt-5.6-sol`。
- 提交提示词时可识别分镜、图片数量、16:9 / 9:16 / 1:1 画面比例与质量指令，自动同步生成设置，并仅将清理后的创作内容发送给模型。
- 新增提示词优化与安全复检：展示优化改动和风险原因，必要时执行第二次安全改写，未通过复检的结果不能替换原提示词。
- 主输入框与优化结果编辑器新增最小、标准、展开三档尺寸控制。

### 🔄 Changed

- 重构连接设置的信息层级和拓扑选择，统一图片与 AI 规划的凭据保存开关；桌面端按启用状态将凭据写入系统凭据管理器。
- 连接配置抽屉固定为 800px 上限宽度，不再随连接模式变化；模型选择器保留上游选项与自由输入能力。
- 自动会话标题改为本地提炼的 24 字短标题，并在加载历史自动标题时重新提炼；手动标题保持不变。

### 🐛 Fixed

- 修复获取模型列表后下拉选项无法选择的问题。
- 修复窄窗口和输入框展开时底部工具栏显示不完整的问题。
- 修复提示词优化弹窗越过桌面标题栏或视口边界，以及优化结果展开尺寸异常的问题。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过。
- `git diff --check` 通过。

## v1.6.0 (2026-08-16)

### 📦 Version

- 项目版本从 `1.5.0` 升级到 `1.6.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.6.0`。

### 🎉 Added

- 创作区新增生成数量选择，支持每轮生成 1 至 9 张图片，默认实际并发为 3，并保留已完成结果与现有取消语义。
- 新增批量结果图库和数量驱动布局：宽屏下 9 张为 3 x 3、8 张为 4 x 2，并在窄屏下按可用空间回流。

### 🔄 Changed

- 批量生成结果与原始用户提示词按同一轮对话分组；重新生成单张图片时仍只请求一张。

### 🐛 Fixed

- 批量生成保留所选画布尺寸，并在返回后校验自然像素比例；发现不符时仅对该槽位串行重试一次，后续未开始任务降为串行。重试仍不符时保留首次原图、实际比例和可见告警，不裁切或静默降级比例。

### ✅ Verify

- `pnpm.cmd typecheck` 通过。
- `pnpm.cmd build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd desktop:check` 通过。
- `git diff --check` 通过。

## v1.5.0 (2026-08-16)

### 📦 Version

- 项目版本从 `1.4.0` 升级到 `1.5.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.5.0`。

### 🎉 Added

- 新增全局受控右键菜单：文本选择可复制，编辑控件支持剪切、复制、粘贴和全选，会话提供重命名与删除操作。
- 生成图片和参考图片新增右键“复制图片”“下载图片”，放大预览工具栏也提供相同操作。

### 🔄 Changed

- 图片放大预览的底部工具栏适配安全区域和窄视口，保留现有变换操作并确保操作栏可见。

### 🐛 Fixed

- 修复右键菜单打开后无法通过菜单外左键关闭的问题；按 `Escape` 也可关闭菜单，且不会阻断目标点击。

### ✅ Verify

- `pnpm.cmd typecheck` 通过。
- `pnpm.cmd build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `git diff --check` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml` 未能在当前 Cygwin Rust 目标完成：Tauri 的 `tao`/`wry` 不支持该平台；Cargo 已在进入平台相关编译前将 `Cargo.lock` 中的 SceneMeld 版本同步为 `1.5.0`。

## v1.4.0 (2026-08-15)

### 📦 Version

- 项目版本从 `1.3.1` 升级到 `1.4.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.4.0`。

### 🎉 Added

- 新增会话重命名：侧栏提供快捷按钮和右键菜单，支持 Enter 保存、Escape 取消及中文输入法组合输入。
- 新增会话自动/手动标题模式；已有本地会话保留原有标题，手动重命名不会被后续消息覆盖。

### 🔄 Changed

- 更新 Web favicon、应用内品牌图标与各桌面平台包图标资源。
- Vite 开发服务器忽略 `src-tauri/target`，避免桌面构建输出触发无关的文件监听更新。

### 🐛 Fixed

- 修复参考图声明格式与二进制内容不一致时图生图请求被本地拒绝的问题；现在会根据 PNG、JPEG、WebP 或 GIF 文件头规范化图片类型，并在桌面端使用检测结果构造请求。
- 将 GitHub Actions 的 Intel macOS runner 从不可用的 `macos-13` 迁移至 `macos-15-intel`，恢复跨平台桌面 CI 与发布构建调度。

### ✅ Verify

- `pnpm.cmd run typecheck` 通过。
- `cargo check --locked --manifest-path src-tauri/Cargo.toml` 通过。
- `pnpm.cmd run check` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- 使用 Rust `1.88.0` 运行 `cargo check --manifest-path src-tauri/Cargo.toml` 通过；`Cargo.lock` 已由 Cargo 同步。

## v1.3.1 (2026-08-15)

### 📦 Version

- 项目版本从 `1.3.0` 升级到 `1.3.1`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.3.1`。

### 🔄 Changed

- 将根目录 `README.md` 重新编审为默认英文文档，调整 Web/Desktop 运行方式、Endpoint 契约、本地数据边界、开发构建与发布说明的表达和结构。
- 新增完整的简体中文 `README.zh-CN.md`，并在中英文文档顶部提供双向语言切换。
- 补充 Tauri updater 公钥、私钥与 GitHub Actions Variables/Secrets 的配置说明，避免混淆公开验证材料和私密签名材料。

### ✅ Verify

- `pnpm.cmd run check` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- 使用隔离 `CARGO_TARGET_DIR` 运行 `cargo check --locked` 通过；未删除现有 `src-tauri/target` 中指向旧工作区的缓存。

## v1.3.0 (2026-08-15)

### 📦 Version

- 项目版本从 `1.2.0` 升级到 `1.3.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.3.0`。

### 🎉 Added

- 新增沉浸式桌面标题栏，提供应用图标、标题、最小化、最大化、关闭、拖拽和双击最大化操作。
- 新增生产态桌面更新：已打包客户端可检查、下载并在用户确认后安装来自 GitHub Release 的签名更新；开发态不会加载 updater。
- 新增独立的外观设置和关于对话框，关于页包含运行版本、开源仓库地址及桌面更新状态。

### 🔄 Changed

- 重组工作区入口、创作引导与连接设置，左侧空间优先用于创作列表；连接信息、外观和关于信息分别呈现。
- 生图请求模型固定为 `gpt-image-2`，画面比例和质量仍可在创作区调整。
- 更新桌面发布工作流、跨平台构建说明、隐私与安全文档，以及产品描述中的兼容性和独立项目声明。

### 🐛 Fixed

- 修复自定义标题栏与 Drawer、Modal、Tooltip、消息提示之间的层级和安全区冲突，标题栏保持在所有应用浮层之上。
- 修复居中关于对话框的垂直定位，并使顶部工作区 Tooltip 和反馈提示避开标题栏。
- 修复桌面端窗口关闭、窗口控制与拖拽区域在无原生装饰模式下的交互衔接。

### ✅ Verify

- `pnpm.cmd run check` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻塞提示。
- `pnpm.cmd run desktop:check` 通过；未启动桌面 GUI 或发布工作流。

## v1.2.0 (2026-08-10 ~ 2026-08-14)

### 📦 Version

- 项目版本从 `1.1.0` 升级到 `1.2.0`。
- `package.json`、Tauri 配置、Rust crate 与 Cargo lockfile 版本统一为 `1.2.0`。

### 🎉 Added

- 新增桌面端图片保存流程，可通过原生保存对话框选择文件位置，并校验文件名、格式与大小。
- 新增 Windows x64、macOS Intel、macOS Apple Silicon 与 Linux x64 的桌面 CI 和 Draft Release 构建矩阵。
- 新增 Rust 1.88 工具链声明及桌面构建所需的平台依赖说明。

### 🔄 Changed

- 桌面端命令封装与图片 Base64 传输逻辑拆分为独立模块，并改进请求取消时序。
- 桌面开发、检查、构建脚本统一通过运行时探测脚本调用 Tauri/Cargo。
- 更新 README 中的多平台桌面开发、构建与发布说明。

### 🐛 Fixed

- Tooltip 内容改为垂直居中。
- 消息轮次、会话、清除本地记录和清除连接配置的确认交互改为居中、不透明的 Modal，并保留不可点击关闭的遮罩。
- 修复桌面端窗口默认最大化及相关玻璃层/主题样式的一致性问题。

### ✅ Verify

- `pnpm.cmd run check` 通过。
- `pnpm.cmd run build` 通过；Vite 保留主包超过 500 kB 的非阻断提示。
- `cargo check --locked --manifest-path src-tauri/Cargo.toml` 通过（Rust `1.88.0`）；未启动桌面 GUI 或发布工作流。

## v1.1.0 (2026-08-10 ~ 2026-08-14)

### 📦 Version

- 项目版本从 `1.0.0` 升级到 `1.1.0`。
- `package.json`、Tauri 配置与 Rust crate 版本统一为 `1.1.0`。

### 🎉 Added

- 新增提供商中立的 `SceneMeld` 品牌、原创开放画框图标、favicon、社交图片与桌面应用图标。
- 新增基于 Tauri 2 的 `SceneMeld Desktop`，通过 Rust 原生 HTTP 直连用户配置的图片 Endpoint，不依赖浏览器 CORS。
- 新增桌面端系统凭据管理器集成，可保存、读取和删除 API Key，且不会把桌面密钥写入 localStorage。
- 新增共享 `ImageTransport` 契约、桌面请求取消、图生图附件传输以及 Base64/远程 URL 图片响应支持。
- 新增 Windows Desktop CI 与未签名 Draft Release 工作流。

### 🔄 Changed

- Web 产品名称从模型相关命名迁移为 `SceneMeld Web`，保留纯静态、浏览器直连和无中转的数据边界。
- 界面改用矿物青单色强调体系，并保留现有 React、StyleX、Ant Design X 布局和毛玻璃交互系统。
- 设置存储键迁移到 `scenemeld.settings.v1`；旧设置会兼容读取，原 IndexedDB 名称继续保留以避免历史会话和图片丢失。
- 更新 README、隐私政策、安全政策、MIT 版权信息和 Web/Desktop 构建说明。

### 🧰 Chore

- 新增 `@tauri-apps/api` 与 `@tauri-apps/cli`，并由 pnpm 同步依赖锁文件。
- 将接近 800 行的应用样式从 `App.tsx` 拆分为独立 StyleX 模块。

### ✅ Verify

- `pnpm check` 通过。
- `pnpm build` 通过；Vite 保留主包超过 500 kB 的非阻断提示。
- `pnpm audit --prod` 通过，未发现已知生产依赖漏洞。
- 敏感信息、旧公开品牌名和源码文件体积扫描通过。
- GitHub Web CI 通过。
- Windows Desktop CI 的前端检查与 Web 构建通过，但 `Check Tauri crate` 步骤以退出码 1 失败；当前环境没有可用的原生 Rust 工具链，详细编译错误需要在 GitHub 已登录页面查看后修复。
